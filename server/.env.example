# Port the server listens on (Railway/Render/Fly inject their own PORT).
PORT=3001
# Allowed client origin for CORS / Socket.IO. Use your deployed client URL in
# production, e.g. https://freestyle-battle.vercel.app. "*" is fine for local.
CLIENT_ORIGIN=*
